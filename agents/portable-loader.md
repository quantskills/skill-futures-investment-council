# Portable Loader

Read `SKILL.md` first.
Use `settings.yaml` for runtime configuration. It defaults to Pandadata live market data.
Use `scripts/` for entrypoints.
Use `references/` for report contracts and output rules.
